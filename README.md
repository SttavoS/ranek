# <center> 🚧 Em Construção... 🚧


## Instalation

```bash
$ git clone https://github.com/SttavoS/ranek.git

$ docker-compose up -d

$ composer install

$ cp .env.example .env

$ php artisan key:generate

$ php artisan migrate
```
